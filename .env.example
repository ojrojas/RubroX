# RubroX — Environment Variables
# Copy to .env and fill in values. NEVER commit .env to source control.

# PostgreSQL
POSTGRES_USER=rubrox
POSTGRES_PASSWORD=your-secure-password-here

# JWT / Security
JWT_SIGNING_KEY=your-256-bit-signing-key-here-use-openssl-rand-base64-32

# ASP.NET Identity
ASPNETCORE_ENVIRONMENT=Production
